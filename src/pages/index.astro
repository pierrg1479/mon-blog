---
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';

const posts = (await getCollection('blog', ({ data }) => {
	return data.draft !== true;
})).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
).slice(0, 3);
---

<!doctype html>
<html lang="fr">
	<head>
		<!-- Google Tag Manager -->
		<script is:inline>
			(function (w, d, s, l, i) {
				w[l] = w[l] || [];
				w[l].push({
					'gtm.start': new Date().getTime(),
					event: 'gtm.js',
				});
				const f = d.getElementsByTagName(s)[0];
				const j = d.createElement(s);
				const dl = l != 'dataLayer' ? '&l=' + l : '';
				j.async = true;
				j.src = 'https://www.googletagmanager.com/gtm.js?id=' + i + dl;
				f.parentNode.insertBefore(j, f);
			})(window, document, 'script', 'dataLayer', 'GTM-PTBBTWQV');
		</script>
		<!-- End Google Tag Manager -->
		<BaseHead
			title="The Optimization Guy | Blog Domotique & Home Assistant"
			description="Blog domotique pour s’amuser avec Home Assistant : idées d’automatisations, intégrations, dashboards, capteurs et bidouilles utiles 🤓🏠"
		/>
	</head>
	<body>
		<!-- Google Tag Manager (noscript) -->
		<noscript
			><iframe
				src="https://www.googletagmanager.com/ns.html?id=GTM-PTBBTWQV"
				height="0"
				width="0"
				style="display:none;visibility:hidden"></iframe
		></noscript>
		<!-- End Google Tag Manager (noscript) -->
		<Header />
		<main>
			<h1>👋 Smart Home Lovers!</h1>
			<p>
				Je partage ici ce que je mets en place chez moi en matière de domotique : automatisations, intégrations Home Assistant, suivi de l'énergie et amélioration du confort au quotidien.
			</p>
			<p>
				Vous y trouverez des cas d'usage détaillés, du code prêt à adapter et des retours d'expérience honnêtes sur ce qui fonctionne… et sur ce qui fonctionne moins bien, le tout avec une obsession : optimiser sans compliquer.
			</p>

			<section class="latest-posts">
				<h2>Dernières publications</h2>
				<ul>
					{posts.map((post) => {
						const slug = post.slug ?? post.id.replace(/\.mdx?$/, '');
						const displayTitle = post.data.h1 ?? post.data.title;

						return (
							<li>
								<a href={`/blog/${slug}/`}>
									{post.data.heroImage && (
										<img
											width="300"
											height="200"
											src={post.data.heroImage.src}
											alt={displayTitle}
											class="post-image"
										/>
									)}
									<div class="content">
										<h3>{displayTitle}</h3>
										<p class="date">
											<FormattedDate date={post.data.pubDate} />
										</p>
										{post.data.description && <p class="description">{post.data.description}</p>}
									</div>
								</a>
							</li>
						);
					})}
				</ul>
			</section>
		</main>
		<Footer />
	</body>
</html>
<style>
	main {
		margin: auto;
		padding: 1rem;
		width: 800px;
		max-width: calc(100% - 2rem);
		color: rgb(var(--gray-dark));
		font-size: 20px;
		line-height: 1.6;
	}
	h1 {
		font-size: 4rem;
		font-weight: 700;
		line-height: 1;
		text-align: center;
		margin-bottom: 1em;
		margin-top: 1em;
	}
	p {
		margin-bottom: 1em;
	}
	.latest-posts {
		margin-top: 3em;
		padding-top: 2em;
		border-top: 1px solid rgb(var(--gray-light));
	}
	.latest-posts h2 {
		font-size: 1.2em;
		font-weight: 700;
		margin-bottom: 1.5em;
	}
	.latest-posts ul {
		list-style-type: none;
		padding: 0;
		display: flex;
		flex-direction: column;
		gap: 2em;
	}
	.latest-posts li {
		border-bottom: 1px solid rgb(var(--gray-light));
		padding-bottom: 1.5em;
	}
	.latest-posts li:last-child {
		border-bottom: none;
	}
	.latest-posts a {
		text-decoration: none;
		color: inherit;
		display: flex;
		gap: 1.5em;
		align-items: flex-start;
	}
        .latest-posts .post-image {
                width: 300px;
                height: 200px;
                object-fit: cover;
                border-radius: 8px;
                flex-shrink: 0;
                transition: transform 0.3s ease;
        }
        .latest-posts a:hover .post-image {
                transform: scale(1.05);
        }
	.latest-posts .content {
		flex: 1;
		min-width: 0;
	}
	.latest-posts h3 {
		font-size: 1.1em;
		font-weight: 700;
		margin: 0 0 0.5em 0;
		color: rgb(var(--black));
		transition: color 0.2s;
	}
	:global(.dark) .latest-posts h3 {
		color: rgb(255, 255, 255);
	}
	.latest-posts a:hover h3 {
		color: rgb(var(--accent));
	}
	.latest-posts .date {
		font-size: 0.875em;
		color: rgb(var(--gray));
		margin: 0 0 0.5em 0;
	}
	.latest-posts .description {
		font-size: 1em;
		line-height: 1.6;
		color: rgb(var(--gray-dark));
		margin: 0;
	}
	:global(.dark) .latest-posts .description {
		color: rgb(209, 213, 219);
	}
	@media (max-width: 768px) {
		.latest-posts a {
			flex-direction: column;
		}
                .latest-posts .post-image {
                        width: 100%;
                        height: 250px;
                }
        }
</style>
